# 当前开发进度与接续说明

最后更新：2026-07-22

当前分支：`main`

当前阶段：`0.5.0` 多层剖面陪伴空间原型

本文是每次推送必须更新的开发快照。目标是在另一台电脑只读取仓库时，也能确定当前已经完成什么、如何恢复环境、下一步从哪里继续，以及哪些状态不会随 Git 同步。

## 本次远端快照

- 分支：`main`
- Q 版房间场景与情境交互：本次提交（见推送后 `git log -1 --oneline`）
- 默认角色定名与旧库迁移：本次提交
- 房间原型与桌宠操作：`5efb847 feat: refine room prototype and pet controls`
- 皮肤管理闭环：`5b3f6ee feat: add persistent skin management`
- 推送前验证：`npm run check` 全部通过，包括 TypeScript、21/21 测试、ESLint 和 Electron 生产构建。
- 补充检查：`git diff --check` 通过；staged diff 未发现 API Key 或桌面 Key 文件路径。

新电脑检出后可运行 `git log -5 --oneline` 对照本节。若远端出现更新，以文件顶部日期和 `main` 最新提交为准。

## 本次进度

- 桌宠新增独立的动作决策层 `pet-action-state.ts`，统一处理 `dragging > speaking > interaction > idle` 优先级；拖拽结束后若气泡仍在，会恢复说话状态而不是直接回到待机。
- `PetSpritePlayer` 新增声明式 `action` 输入，皮肤切换、播放器初始化和动作变化都使用同一播放入口；尚未提供的动作资源继续由主进程安全回退到 `idle`，因此可以先落地状态链路再逐步补素材。
- 新增 2 项桌宠动作优先级测试，当前自动化测试总数为 30 项。
- 新增 `docs/multi-floor-space.md`，明确三层建筑、房间职责、情境映射、节点路径、交互层级、动作素材约束和 `0.5.0` 验收标准。
- 新增纯 TypeScript 多层空间模型 `multi-floor-state.ts`：`1200 × 900` 逻辑画布，包含观测室、温室、档案室、实验室、书房、厨房、起居室和卧室八个空间。
- 四种现有情境分别映射到起居室、实验室、厨房和卧室；跨楼层移动会经过中央升降井的楼层节点，同层移动直接前往目标锚点。
- 新增 `MultiFloorScene.vue` 并接入陪伴空间：使用程序化剖面、房间标签、简化家具和 Q 版角色验证整体空间；点击任意房间会请求角色前往。
- 对话中的 thinking/speaking 会暂停跨房间移动，回到 idle 后继续路径；自动化测试覆盖情境映射、同层/跨层路径、到达后动作、对话暂停恢复和电梯途中改选目标。
- 已通过本机 WisArt `gpt-image-2` 通道生成两版 `1536 × 1024`、`quality=low` 概念图。第一版验证多层氛围但重复绘制角色；第二版只保留一名角色，并强化中央电梯、楼梯、楼板和房间分隔，现作为多层空间视觉基准。
- 使用现有 4K 角色参考图生成牧濑红莉栖 Q 版设定板，包含正面、侧面、背面、三分之四视角和三种表情；角色约三头身，并保留长发、蓝眼、红领带和棕色外套等小尺寸识别点。
- 已用 Q 版设定板作为参考生成 `4 × 2`、8 帧向右行走 Sheet。原始色键图转换为透明 Alpha 后，通过新增的 `scripts/normalize-sprite-sheet.py` 自动切格并统一人物高度、水平中心和脚底基线，输出到 `resources/animation-prototypes/makise-kurisu-chibi/walk-right.png`。
- 新增 `docs/character-animation.md`，明确逻辑移动、朝向、动作衔接、跨楼层电梯流程、精灵锚点和首批动作资源规格。当前行走资源属于可播放原型，仍需实机检查头发、外套和五官的逐帧漂移后才能纳入正式皮肤。
- 多层场景中的程序化角色剪影已经替换为 Q 版 8 帧行走 Sheet。同层移动播放行走循环，向左暂用水平镜像；停止、思考、说话和乘电梯时使用稳定帧。
- 跨楼层移动新增 `walking / elevator / idle` 表现阶段。角色抵达中央升降井后进入原创玻璃轿厢，到站后继续前往目标房间；由于乘梯过渡很短，状态卡保持原有移动状态，不再单独显示乘梯文案。
- 电梯节点与轿厢移除黄色边框，改为低对比度灰蓝标识，避免短暂过渡抢占角色和房间的视觉焦点。
- 升降井现在明确显示 `3F 观测层 / 2F 研究层 / 1F 生活层` 三个停靠节点。电梯取消连续垂直插值，角色会在经过的节点随轿厢短暂露面，再跳到下一个节点，不会把垂直移动表现成普通行走。
- 修复移动途中重新选择跨层目标时偶发斜线移动的问题：路线现在从角色实际位置和最近楼层重新规划，先水平回到当前层电梯节点，再进行离散楼层跳转，最后水平前往目标房间。
- 多层状态机新增 walking/elevator 阶段、离散节点跳转和电梯途中改选目标测试；完整 `npm run check` 已通过，当前自动化测试总数为 28 项。

- 修复桌宠设为 `240%` 后进入陪伴空间产生横向偏移、裁切，以及返回桌宠时残留房间一角的问题。Electron 页面缩放现在永久保持 `1`，桌宠改为固定 `360 × 440` 逻辑画布并在渲染层使用 CSS 缩放，房间视口不再继承桌宠比例。
- 返回桌宠的顺序改为先关闭设置并让 Vue 完成 `RoomScene` 卸载，再恢复 Electron 桌宠窗口边界，避免小窗口截取尚未卸载的房间页面。
- `RoomScene` 与 `PetSpritePlayer` 增加异步初始化取消保护；卸载时依次停止 ticker、移除回调、断开 `ResizeObserver`、销毁精灵和移除显示对象，最后销毁 renderer，不再深度销毁仍被 PixiJS 渲染链引用的对象。
- 修复返回桌宠时默认 CSS Q 版角色短暂闪现后才显示实体皮肤的问题。皮肤状态现在区分 `loading / ready / unavailable`，加载期间保持透明，只有实体资源确认不可用时才展示降级形象。
- 设置页皮肤预览同步采用三态加载，切换或重新扫描皮肤时不再短暂显示“无法预览”；运行中换皮仍保留旧实体直到新资源加载完成。
- 用户已实机确认返回桌宠的 Q 版闪现问题解决；本轮 `npm run check` 全部通过，包括 TypeScript、21/21 测试、ESLint 和 Electron 生产构建。

- 陪伴空间已从 CSS「雨夜书房」升级为 Q 版 PixiJS 模拟经营式房间场景（`src/renderer/src/RoomScene.vue`）：`960 × 640` 逻辑坐标，按脚底 Y 值深度排序，家具分前后层，角色在其间穿行。
- 房间行为逻辑抽离为纯 TypeScript 状态机（`src/renderer/src/room-state.ts`），与 PixiJS 渲染分离，可独立单元测试；共 5 项测试覆盖行走区域裁剪、情境锚点映射、到达后动作切换和对话打断优先级。
- 动作状态机已支持 `idle`、`walk`、`sit`、`look-around`、`thinking`、`speaking`、`studying`、`eating`、`resting`、`interaction`，并带优先级、锁定时长、阻塞与回退；对话中的 thinking/speaking 会临时打断低优先级的环境动作，结束后回到当前情境。
- 陪伴空间提供四个明确的情境入口（自由陪伴 / 一起学习 / 一起吃饭 / 休息一下）：切换后角色会走向对应家具锚点（书桌 / 餐桌 / 阅读椅 / 房间中央）并进入对应活动，情境选择通过 `localStorage` 持久化。
- 自由陪伴且无对话时，角色会以低频（约 22–46 秒）触发看窗等环境动作；系统要求减少动态效果（`prefers-reduced-motion`）时自动停止。
- 点击地毯可让角色走到指定位置，点击家具会走到对应锚点并开始活动；书桌显示书本、餐桌显示碗、思考/说话显示气泡、休息显示 `z Z`，状态卡片实时显示当前动作说明。
- 房间对话状态由聊天流式状态推导：仅在思考时显示 thinking，收到首个流式内容后切到 speaking，空闲回到 idle。
- 默认角色已正式定名为「牧濑红莉栖」：更新了默认角色种子、托盘提示、界面称呼、头像字样、开发皮肤名称（`牧濑红莉栖开发皮肤`）和相关文档。
- 数据库新增 version 3 迁移：把旧的 `chengxia` 角色无损迁移为 `makise-kurisu`，保留羁绊等级、经验和全部对话历史；已有专门单元测试覆盖。
- Q 版形象特征：红棕长发、蓝眼、白衬衫 + 红领带、棕色外套、深色下装，房间采用暖米色墙面、木色家具与粉灰地毯的动漫化配色。
- 现有参考角色仍具有仓库内可追踪的 4K RGBA 母版和 `4096 × 2048`、8 帧待机 Sheet；桌宠继续使用高清全身形态，陪伴空间使用同一角色的 Q 版形态。
- `scripts/build-reference-idle.py` 可完全在本地从 4K 母版重建待机动画，不调用图片 API，也不消耗生图 Token。
- 场景与素材保持离线可用，不调用图片生成 API；Q 版角色与家具目前为程序化 PixiJS 绘制，后续可替换为生成素材。
- 日常联动规划仍分两层：动作系统先支持学习、用餐、休息等本地表现（已完成手动情境入口）；Agent 接入后再把「查资料」等表现升级为真实搜索、整理与任务反馈。
- 皮肤 `canvas.hitbox` 已映射为人物拖拽区，人物、按钮和气泡关闭按钮之外的透明窗口区域会把鼠标交还给下层应用。
- 默认问候气泡已移除；角色只在实际 `say` 时显示消息，并按非空白字符数在 `2.4–9` 秒后淡出。
- 设置页已增加皮肤动画预览、选择持久化、打开用户目录、手动重新扫描和损坏皮肤错误列表；桌宠与陪伴空间会立即同步新选择。
- 已选皮肤被移除或损坏时会自动回退，皮肤根目录无法读取时也不会中断其他目录扫描。
- 陪伴空间已经移除外层透明留白，窗口最大化后内容铺满可用区域；角色画布、显示尺寸和脚底锚点会随舞台尺寸重新计算。
- 首次进入陪伴空间会先解除桌宠最大尺寸限制，再设置房间最小尺寸；初始房间边界按桌宠所在显示器的工作区收敛，避免 768p 屏幕上人物下半身和设置按钮被任务栏或屏幕边缘裁掉。
- 房间边界与最小尺寸计算已拆为纯函数，并覆盖大工作区、768p 工作区和带负坐标副屏，后续调整窗口常量时可自动发现越界回归。
- 陪伴空间工作区的 Grid 行与三个主面板均允许收缩到窗口可用高度；长聊天历史改为只在消息区内部滚动，不再把角色小腿、快捷操作和侧栏设置按钮推到窗口下方。
- PixiJS 播放器初始化完成后会按最新 props 再同步一次渲染器尺寸，避免首次进入房间时父布局更新早于 Pixi 初始化完成，导致角色按大舞台定位却被旧的 `600 × 712` 画布裁掉右半身和小腿。
- 本轮已完成冷启动首次双击进入房间的实机验收：普通窗口内角色全身、快捷操作、侧栏设置入口和聊天输入框均完整可见；长聊天历史保持在消息区内部滚动。
- 呼吸幅度后续只做小幅视觉微调，不应再次扩大胸腹主运动。

## 新电脑恢复步骤

```powershell
git clone git@github.com:soy-saber/virtual-bond.git
Set-Location virtual-bond
npm ci
npm run check
npm run dev
```

如需重建参考待机 Sheet，额外安装本地图片处理依赖：

```powershell
py -m pip install Pillow numpy
py scripts/build-reference-idle.py
```

重建脚本会覆盖 `resources/skins/reference-companion/animations/idle.png`。执行后应运行 `npm run check` 并实机确认脚底不漂移、下半身不呼吸、胸肩和头颈只有轻微联动。

## 不会随仓库同步的状态

- API Key 不在仓库中。普通聊天 Key 由应用通过 Electron `safeStorage` 保存在本机；换电脑后需要在设置中重新填写或重新导入 CCSwitch 配置。
- 本地验收默认使用 DeepSeek 直连，而不是 Codex 中转；不要把桌面上的 Key 文件路径、内容或解密后的值写入仓库、日志和文档。
- 图片生成 Key 与聊天 Key 相互隔离。任何真实生图调用都必须先说明模型、尺寸、质量、参考图数量和用途，并取得用户明确批准。
- SQLite 对话、角色和设置数据位于 Electron 的本机 `userData` 目录，不通过 Git 同步。新电脑读取仓库只能恢复代码与内置资源，不能自动恢复本机对话历史。
- Agent 能力尚未接入应用；规划通过 OpenCode CLI 提供，并与普通聊天 API 分离。

## 当前验收入口

1. 启动后确认桌宠待机动画能够播放，脚底与下半身保持稳定。
2. 在 `45%–240%` 范围调整桌宠比例，并反复执行桌宠 → 陪伴空间 → 桌宠，确认房间不偏移、不裁切，返回后不残留房间画面，也不会先闪现默认 CSS Q 版角色。
3. 在人物外透明区域点击下层应用，确认只有人物包围盒、底部按钮和可见气泡关闭按钮拦截鼠标。
4. 冷启动后首次双击角色进入陪伴空间，确认 Q 版房间场景铺满可用区域，牧濑红莉栖全身、状态卡片、情境按钮和输入框均可见；再覆盖最大化、连续拖动调整尺寸和返回桌宠。
5. 在陪伴空间点击地毯让角色走到指定位置，点击书桌、餐桌、阅读椅和雨窗确认她会走过去并切换到对应动作；再切换“自由陪伴 / 一起学习 / 一起吃饭 / 休息一下”情境按钮，确认状态卡片文案随之更新，且刷新后仍记住上次情境。
6. 发送消息时确认房间角色进入 `thinking` / `speaking` 表现并临时打断当前活动，回复结束后回到所选情境。
7. 使用 DeepSeek 直连发送消息，确认流式文本、停止生成、消息气泡自动淡出和历史恢复正常。
8. 首次以旧版 `chengxia` 数据库启动，确认角色被无损迁移为 `makise-kurisu`（牧濑红莉栖），对话历史和羁绊等级保留。
9. 运行 `npm run check`，保证类型检查、21/21 测试、Lint 和生产构建全部通过。

## 下一步开发顺序

1. 将当前单层 Q 版房间重新设计为多层建筑剖面空间，建立强角色轮廓、跨房间移动、环境叙事与浮层交互的概念图和规格。
2. 为房间补充可编辑时间日程，让情境可以按用户设定的作息自动切换；仍不通过前台应用监控推断用户状态。
3. 扩展动作状态机表现层并制作动作精灵图规格：为 `studying`、`eating`、`resting` 增加分段动画、随机小动作和跨房间流程。
4. 用真实角色与多层房间素材替换当前程序化占位形象；实际生成仍需逐次批准，并先补齐图片生成设置、参考图选择、任务进度与历史记录。
5. 通过 OpenCode CLI 接入隔离的 Agent 能力，把学习情境下的“查资料”等表现动作升级为真实搜索、整理和任务反馈。
6. 补齐 `0.3.0` 遗留的 Provider 单元测试与关键 IPC 集成测试。
7. 开发日程习惯记忆、长期记忆与关系成长。

## 推送维护约定

每次推送前必须：

- 更新 README 的已实现功能、当前优先级和已知问题。
- 更新本文件的日期、本次进度、验收状态和下一步。
- 把新遇到的问题及解决方案追加到 `docs/troubleshooting.md`。
- 同步更新受影响的设计文档，不能只改代码。
- 运行 `npm run check` 和 `git diff --check`。
- 检查 staged diff 中没有 API Key、本机绝对 Key 路径或不应公开的原始素材。
